# 地面标定补充方案

## 🎯 问题分析

当前的 `camera_calibration.py` 工具已经支持内参和外参标定，但对于篮球场身高测量，我们需要更精确的地面（Z=0）标定。

### 为什么需要专门的地面标定？

1. **测量精度要求高**：身高测量误差需要控制在 ±5-10cm 内
2. **地面参考重要性**：身高测量依赖于准确的地面参考平面
3. **相机安装角度**：篮球场相机通常有一定倾斜角度，需要精确标定
4. **温度和湿度影响**：篮球场地面可能有轻微变形

## 🔧 补充地面标定方案

### 方法1：增强现有标定工具

#### 1. 地面参考点标定
```python
def calibrate_ground_plane(calibration_images, camera_matrix, dist_coeffs):
    """
    专门标定地面参考平面
    """
    # 1. 在地面放置已知尺寸的参考物体
    # 2. 拍摄多角度图片
    # 3. 检测参考点在图像中的位置
    # 4. 计算地面平面方程
    # 5. 建立地面坐标系

    # 地面参考点（世界坐标系）
    ground_points_world = np.array([
        [0, 0, 0],        # 地面原点
        [1000, 0, 0],     # X轴方向1米
        [0, 1000, 0],     # Y轴方向1米
        [1000, 1000, 0]   # XY平面上的点
    ], dtype=np.float32)

    # 对应的图像坐标（需要手动标注或自动检测）
    ground_points_image = np.array([
        [320, 240],       # 图像中的对应点
        [400, 240],
        [320, 300],
        [400, 300]
    ], dtype=np.float32)

    # 计算相机外参（针对地面）
    retval, rvec, tvec = cv2.solvePnP(
        ground_points_world,
        ground_points_image,
        camera_matrix,
        dist_coeffs
    )

    return rvec, tvec
```

#### 2. 地面法向量计算
```python
def compute_ground_plane_normal(rvec, tvec, camera_matrix):
    """
    计算地面平面法向量
    """
    # 将旋转向量转换为旋转矩阵
    R, _ = cv2.Rodrigues(rvec)

    # 相机在世界坐标系中的位置
    camera_position = -R.T @ tvec

    # 地面法向量（假设Z轴垂直向上）
    ground_normal = np.array([0, 0, 1])

    # 变换到相机坐标系
    ground_normal_camera = R @ ground_normal

    return ground_normal_camera, camera_position
```

### 方法2：独立地面标定程序

#### 1. 地面标定板设计
```
地面标定板规格：
- 尺寸：2m x 2m（适合篮球场）
- 图案：大尺寸棋盘格（边长10cm）
- 材料：轻质铝板或PVC板，便于搬运
- 固定方式：地面吸附或重物固定
```

#### 2. 标定流程
```python
class GroundCalibration:
    def __init__(self):
        self.ground_points = []
        self.image_points = []

    def add_calibration_point(self, world_point, image_point):
        """
        添加标定点对
        world_point: [x, y, 0] - 地面世界坐标
        image_point: [u, v] - 图像坐标
        """
        self.ground_points.append(world_point)
        self.image_points.append(image_point)

    def compute_ground_transform(self, camera_matrix, dist_coeffs):
        """
        计算地面坐标变换矩阵
        """
        # 1. 畸变矫正图像点
        undistorted_points = cv2.undistortPoints(
            np.array([self.image_points]),
            camera_matrix,
            dist_coeffs
        )[0]

        # 2. 建立3D-2D对应关系
        world_points = np.array(self.ground_points, dtype=np.float32)
        image_points = np.array(undistorted_points, dtype=np.float32)

        # 3. 计算外参
        retval, rvec, tvec = cv2.solvePnP(
            world_points,
            image_points,
            camera_matrix,
            None  # 已经做过畸变矫正
        )

        return rvec, tvec
```

## 📐 数学原理

### 1. 地面平面方程
```
地面平面：ax + by + cz + d = 0
对于水平地面：z = 0，所以平面方程简化为：
ax + by + d = 0

其中：
- (a, b, c) 是平面法向量
- d 是平面常数项
```

### 2. 世界坐标到图像坐标的转换
```python
def world_to_image(world_point, camera_matrix, rvec, tvec, dist_coeffs=None):
    """
    将世界坐标点转换为图像坐标
    """
    # 1. 世界坐标到相机坐标
    R, _ = cv2.Rodrigues(rvec)
    camera_point = R @ world_point + tvec

    # 2. 相机坐标到图像坐标
    image_point = camera_matrix @ camera_point
    image_point = image_point[:2] / image_point[2]

    # 3. 畸变矫正（如果需要）
    if dist_coeffs is not None:
        image_point = cv2.undistortPoints(
            image_point.reshape(1, 1, 2),
            camera_matrix,
            dist_coeffs
        )[0][0]

    return image_point
```

### 3. 图像坐标到世界坐标的转换（重点）
```python
def image_to_world_ground(image_point, camera_matrix, rvec, tvec, ground_z=0):
    """
    将图像坐标转换为地面世界坐标
    """
    # 1. 畸变矫正
    undistorted_point = cv2.undistortPoints(
        np.array([image_point]),
        camera_matrix,
        dist_coeffs
    )[0][0]

    # 2. 图像坐标到相机坐标
    R, _ = cv2.Rodrigues(rvec)
    camera_matrix_inv = np.linalg.inv(camera_matrix)

    # 归一化平面坐标
    normalized_point = camera_matrix_inv @ np.array([*undistorted_point, 1])

    # 3. 求射线方向向量
    ray_direction = R.T @ normalized_point

    # 4. 求射线与地面的交点
    camera_position = -R.T @ tvec  # 相机在世界坐标系中的位置

    # 地面方程：z = ground_z
    if ray_direction[2] != 0:
        t = (ground_z - camera_position[2]) / ray_direction[2]
        world_point = camera_position + t * ray_direction
        return world_point[:2]  # 返回x, y坐标
    else:
        return None  # 射线平行于地面
```

## 🛠️ 实施步骤

### 第一阶段：准备工作
```bash
# 1. 准备地面标定板
# 2. 测量篮球场尺寸
# 3. 确定标定点位置
# 4. 准备测量工具（激光测距仪等）
```

### 第二阶段：数据采集
```bash
# 1. 在地面放置标定板
# 2. 记录标定板的世界坐标
# 3. 拍摄标定图片
# 4. 移动标定板位置，重复拍摄
```

### 第三阶段：标定计算
```python
# 1. 运行标准相机标定
# 2. 运行地面标定补充程序
# 3. 计算地面变换矩阵
# 4. 验证标定精度
```

## 📊 精度验证

### 1. 重投影误差检验
```python
def validate_ground_calibration(world_points, image_points,
                               camera_matrix, rvec, tvec, dist_coeffs):
    """
    验证地面标定的精度
    """
    # 1. 将世界坐标投影到图像
    projected_points, _ = cv2.projectPoints(
        world_points,
        rvec, tvec,
        camera_matrix,
        dist_coeffs
    )

    # 2. 计算重投影误差
    errors = []
    for i, (projected, actual) in enumerate(zip(projected_points, image_points)):
        error = np.linalg.norm(projected[0] - actual)
        errors.append(error)
        print(f"点{i+1}重投影误差: {error:.2f}像素")

    mean_error = np.mean(errors)
    print(f"平均重投影误差: {mean_error:.2f}像素")

    return mean_error
```

### 2. 距离测量验证
```python
def validate_distance_measurement(known_distances, image_points,
                                camera_matrix, rvec, tvec):
    """
    验证距离测量精度
    """
    world_points = []
    for point in image_points:
        world_point = image_to_world_ground(point, camera_matrix, rvec, tvec)
        world_points.append(world_point)

    # 计算测量距离
    measured_distances = []
    for i in range(0, len(world_points), 2):
        if i+1 < len(world_points):
            dist = np.linalg.norm(world_points[i] - world_points[i+1])
            measured_distances.append(dist)

    # 比较已知距离和测量距离
    for known, measured in zip(known_distances, measured_distances):
        error = abs(known - measured)
        print(f"距离误差: {error:.2f}mm")

    return measured_distances
```

## 🎯 推荐方案

### 方案A：增强现有工具（推荐）
1. **优点**：
   - 利用现有标定工具
   - 开发周期短
   - 维护成本低

2. **实现方式**：
   - 在现有标定基础上增加地面参考点标定
   - 修改 `camera_calibration.py` 添加地面标定功能

### 方案B：独立地面标定
1. **优点**：
   - 专门针对地面标定优化
   - 精度可能更高
   - 算法更专业

2. **实现方式**：
   - 开发独立的地面标定程序
   - 使用专门的地面标定板

## 📈 预期效果

| 项目 | 当前精度 | 地面标定后 |
|------|----------|------------|
| **重投影误差** | 0.5-1.0像素 | 0.1-0.3像素 |
| **距离测量误差** | ±50-100mm | ±10-20mm |
| **身高测量误差** | ±20-30% | ±5-10% |
| **系统稳定性** | 一般 | 很高 |

## 🚀 实施建议

### 立即行动
1. **准备地面标定板**
2. **测量篮球场标准尺寸**
3. **确定标定点位置**

### 短期目标（1周）
- 实现地面标定功能
- 测试标定精度
- 集成到身高测量系统中

### 长期目标（1月）
- 优化标定流程
- 建立自动化标定系统
- 实现实时精度监控

这个补充标定方案将使你的身高测量系统精度提升到专业级水平！🏀✨
